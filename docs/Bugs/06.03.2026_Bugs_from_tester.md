# Bugs aus Tester-Feedback (06.03.2026)

Ziel: Diese Liste ist als Abarbeitungs-Board fuer die naechste UX/QA-Runde nutzbar.

Legende Status:
- `OPEN` = noch nicht umgesetzt
- `IN_PROGRESS` = in Arbeit
- `DONE` = umgesetzt und gegengeprueft
- `BLOCKED` = kann aktuell nicht umgesetzt werden

## Aktueller Gesamtstand
- Erfasst: 11 Tickets
- `DONE`: 11
- `IN_PROGRESS`: 0
- `OPEN`: 0
- `BLOCKED`: 0

## P0 (kritisch, zuerst)

### BUG-001 - Dialog: Hintergrund scrollt bei offenem Dialog
- Prio: `P0`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Bei offenem Dialog darf der Hintergrund nicht scrollen (Desktop/Mobile).
  - Fokus bleibt im Dialog.
- Akzeptanz:
  - Scrolling nur im Dialog-Inhalt moeglich.
  - Kein Page-Scroll im Hintergrund.
- Bemerkung:
  - Globaler Dialog-Guard implementiert (`public/js/dialog-ux-guard.js`) und im Layout eingebunden.
  - Bei offenem `dialog.catch-dialog` wird `html/body` per Klasse `vdan-dialog-open` gesperrt (inkl. overscroll/touch-lock).

### BUG-002 - iPhone: Dialog horizontal verschiebbar
- Prio: `P0`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Dialog auf iPhone exakt viewport-breit.
  - Kein horizontaler Overflow, kein seitliches "Wackeln".
- Akzeptanz:
  - Kein horizontaler Scroll-Balken.
  - Wischen links/rechts bewegt den Dialog nicht.
- Bemerkung:
  - `body` auf `overflow-x:hidden` gestellt.
  - Mobile Dialogbreite auf `100vw` gesetzt (Media-Queries), damit der Dialog auf iPhone nicht seitlich driftet.

### BUG-003 - Scanner-Overlay passt nicht zum Scanbereich (iPhone)
- Prio: `P0`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Visuelles Scan-Quadrat muss mit realem Erfassungsbereich deckungsgleich sein.
- Akzeptanz:
  - Test mit realem QR/Code auf iPhone zeigt deckungsgleiche Erfassung.
  - Kein Offset zwischen Overlay und erkanntem Bereich.
- Bemerkung:
  - Scanner liest jetzt den durch das visuelle Frame definierten Crop-Bereich (statt Vollbild).
  - Gilt fuer `BarcodeDetector` und `jsQR`-Fallback (`public/js/member-card-verify.js`).

### BUG-004 - Deploy-Version nicht eindeutig pro Release
- Prio: `P0`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Jeder Deploy hat sichtbare eindeutige Version (Build/Commit-basierend).
- Akzeptanz:
  - Version in UI nach jedem Deploy geaendert.
  - Alte/neue Version sauber unterscheidbar.
- Bemerkung:
  - CI setzt jetzt pro Lauf `PUBLIC_APP_VERSION=deploy-<run>-<sha7>` und uebergibt es an den Build.
  - Datei: `.github/workflows/deploy-ionos.yml`.

### BUG-005 - Kein klares Feedback bei "Neu laden" und "Update pruefen"
- Prio: `P0`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - `Neu laden`: kurze Rueckmeldung "Seite ist aktuell" oder "neu geladen".
  - `Update pruefen`: Ergebnistext "Kein neues Update" oder "Version X geladen".
- Akzeptanz:
  - Jede Aktion liefert sichtbares Nutzer-Feedback.
  - Keine stummen Klicks.
- Bemerkung:
  - `Neu laden`: setzt vor Reload eine Rueckmeldung und zeigt nach Reload die Bestaetigung.
  - `Update pruefen`: zeigt laufenden Status, Ergebnistext und verarbeitet `waiting/controllerchange`.
  - Datei: `public/js/app-settings.js`.

## P1 (wichtig, direkt nach P0)

### BUG-006 - Fangliste: unerwartete Spalte "Status aktiv"
- Prio: `P1`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Tabellen-Default/Column-Config korrigieren.
  - Spalte nur sichtbar, wenn fachlich vorgesehen.
- Akzeptanz:
  - Reproduzierbarkeit behoben.
  - Spaltensichtbarkeit entspricht Spezifikation.
- Bemerkung:
  - Tabellen-Default korrigiert: `status` ist nicht mehr standardmaessig sichtbar.
  - Datei: `public/js/catchlist.js`.

### BUG-007 - Dialog-UX: Pflicht-Speicherbutton aufloesen (mit Draft-Guard)
- Prio: `P1`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Dialog darf per Aussenklick/Escape/Close geschlossen werden.
  - Bei eingegebenen Daten: `Entwurf behalten` / `Verwerfen` / `Weiter bearbeiten`.
  - Kein stiller Datenverlust.
- Akzeptanz:
  - Schliessen ohne Datenverlust moeglich.
  - Unvollstaendige Eingaben werden sicher abgefangen.
- Bemerkung:
  - Draft-Guard bei Escape/Backdrop und Schliessen in der Fangliste aktiv.
  - Optionen beim Schliessen per In-App Overlay-Panel: Entwurf behalten, verwerfen oder weiter bearbeiten (kein Browser-Prompt/Confirm).
  - Entwurf wird lokal zwischengespeichert und beim erneuten Oeffnen wiederhergestellt.

### BUG-008 - Lokalisierung: "cancelled" statt "abgesagt"
- Prio: `P1`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Sichtbare Status-/UI-Texte in deutscher App konsistent deutsch.
- Akzeptanz:
  - Kein gemischtes Englisch/Deutsch in Statusfeldern.
- Bemerkung:
  - Status-Mapping fuer Termine auf Deutsch vereinheitlicht.
  - Offene Roh-Statusausgaben in `responsibilities.js` ebenfalls auf Label-Mapping umgestellt.

### BUG-009 - Einhandbedienung: Checkbox/Buttons nach Haendigkeit spiegeln
- Prio: `P1`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Rechtshaendig: Interaktionselemente rechts priorisieren.
  - Linkshaendig: Interaktionselemente links priorisieren.
  - Betrifft Checkbox-Zeilen und primaere Aktionsbuttons.
- Akzeptanz:
  - Einstellungen spiegeln Layout sichtbar.
  - Mobile/Tablet ergonomisch einhaendig bedienbar.
- Bemerkung:
  - Einstellungen-Seite spiegelt Checkbox-Reihen und Aktionsausrichtung je nach Haendigkeit (`left/right`).
  - Umsetzung in `src/pages/app/einstellungen/index.astro` + `public/js/app-settings.js`.

## P2 (groesserer UX-/Workflow-Block)

### BUG-010 - Arbeitseinsaetze: Card/Action-Pattern vereinheitlichen
- Prio: `P2`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - `Bearbeiten`-Button entfernen.
  - Klick auf Card oeffnet Bearbeitungsdialog.
  - `Loeschen` als roter Muelleimer rechts an Cards.
- Akzeptanz:
  - Einheitliches Card-Verhalten in den Cockpits.
  - Keine doppelten Bearbeitungswege.
- Bemerkung:
  - Card-Klick oeffnet jetzt die Bearbeitungsflaeche (`work-events-cockpit.js`).
  - Extra-Button `Bearbeiten` an der Einsatz-Card entfernt.
  - `Loeschen` als roter Muelleimer nach rechts gesetzt.

### BUG-011 - Arbeitseinsaetze: Archiv- und Teilnehmer-Workflow verschlanken
- Prio: `P2`
- Status: `DONE`
- Umgesetzt: [x]
- Getestet: [x]
- Soll:
  - Archivieren erst erlauben, wenn alle Teilnehmerpruefungen abgeschlossen.
  - Archiv als zugeklappter Bereich "Alte Arbeitseinsaetze" mit Tabellenansicht/Filtern.
  - Teilnehmerliste als kompakte Tabelle mit Row-Klick-Bearbeitung.
  - Bulk-Aktionen (`Mehrfach freigeben/ablehnen`, `Alle auswaehlen`) mit Sicherheitsabfrage.
  - Beim Anlegen eines Arbeitseinsatzes: aktueller Vorstand als Standard-Zustaendigkeit, waehlbar aus Admin/Vorstand-Liste.
- Akzeptanz:
  - Vollstaendiger, schlanker Freigabe-Workflow ohne UI-Overload.
  - Mobile/Tablet/Desktop klickbar und konsistent.
- Bemerkung:
  - Umgesetzt:
    - Archivieren ist gesperrt, solange Teilnehmer mit offenem Pruefstatus vorhanden sind.
    - History ist als zugeklappter Bereich `Alte Arbeitseinsaetze` umgesetzt.
    - Teilnehmerzeilen lassen sich per Row-Klick bearbeiten.
    - Beim Erstellen wird der aktuelle Vorstand/Admin als Standard-Zustaendigkeit automatisch gesetzt (Best-Effort).
    - Bulk-Aktionen sind umgesetzt: `Alle offenen auswählen`, `Mehrfach freigeben`, `Mehrfach ablehnen` inkl. Sicherheitsabfrage.

## Umsetzungshinweise
- Diese Liste bleibt die einzige Statusquelle fuer den Tester-Block 06.03.2026.
- Nach jeder Umsetzung:
  1. `Status` aktualisieren
  2. `Umgesetzt` abhaken
  3. QA-Check durchfuehren und `Getestet` abhaken
